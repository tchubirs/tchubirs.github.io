# Núcleo — sempre necessário
anthropic>=1.0.0
pydantic>=2.7
PyYAML>=6.0
requests>=2.32

# Ingestão de vídeo/legenda da fonte licenciada da campanha
yt-dlp>=2026.1.1

# ffmpeg estático (evita depender do ffmpeg do sistema; funciona no GitHub Actions)
imageio-ffmpeg>=0.5.1

# Publicação YouTube (API oficial Data API v3)
google-api-python-client>=2.130.0
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Transcrição local — só usada quando a fonte NÃO tem legenda pronta.
# Opcional: instale com `pip install -r requirements-whisper.txt`
